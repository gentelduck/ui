import { Badge } from '@gentleduck/registry-ui-duckui/badge'

export default function Badge2Demo() {
  return (
    <Badge arial-label="Badge" role="button" variant={'default'}>
      Badge
    </Badge>
  )
}
