import { RawConfigType } from '../duck-benchmark/src/utils/get-project-config/get-project-config.dto'

const config: RawConfigType = {
  src: './src',
  out_dir: './dist',
  show_log: false,
}

export default config
