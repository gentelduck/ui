import { Badge } from '@/registry/registry-ui-components'

export default function Badge4Demo() {
  return (
    <Badge
      arial-label="Badge"
      role="button"
      variant={'outline'}
    >
      Badge
    </Badge>
  )
}
