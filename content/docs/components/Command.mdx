---
title: Command
description: Fast, composable, unstyled command menu for React.
component: true
links:
  doc: https://cmdk.paco.me
---

<ComponentPreview
  name="CommandMainDemo"
  align="start"
  className="[&_.preview>div]:max-w-[450px]"
  description="A command menu"
  showSettings={false}
/>

## Features

- **Searchable Command Dialog:** Command palette with search functionality.
- **Groupable Command List:** Organize commands into groups with optional headings.
- **Selectable Items:** Items support actions, selection, and checkmarks.
- **Keyboard Navigation:** Navigate and select commands via keyboard.
- **Responsive Design:** Adapts to various screen sizes.

## About

The `<Command />` component uses the [`cmdk`](https://cmdk.paco.me) component by [pacocoursey](https://twitter.com/pacocoursey).

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add command
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install cmdk 
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="Command" />

<Step>The Combobox is built using a composition of the `<Button/>`, `<Dialog/>`, `<ScrollArea/>` and the `<Tooltip/>` components.</Step>

<Step>You also will need to install the [`Button`](/docs/components/Command#installation),
    [`Tooltip`](/docs/components/Tooltip#installation),
    [`Dialog`](/docs/components/Dialog#installation) and [`ScrollArea`](/docs/components/ScrollArea#installation) components because some dependencies are inherited from them.</Step>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from '@/components/ui'
```

```tsx
<Command>
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Search GitHub</CommandItem>
      <CommandItem>Search Twitter</CommandItem>
      <CommandItem>Search Discord</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>General</CommandItem>
      <CommandItem>Profile</CommandItem>
      <CommandItem>Notifications</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

## Examples

### List Group 

<ComponentPreview name="CommandGroupDemo" description="A component dialog." showSettings={false} />

### Dialog

<ComponentPreview name="CommandDialogDemo" description="A component dialog." showSettings={false} />

To show the command menu in a dialog, use the `<CommandDialog />` component, or use the `<Button/>` component with the `command` variant.

```tsx
export function CommandMenu() {
  const [open, setOpen] = React.useState(false)

  React.useEffect(() => {
    const down = (e: KeyboardEvent) => {
      if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault()
        setOpen(open => !open)
      }
    }
    document.addEventListener('keydown', down)
    return () => document.removeEventListener('keydown', down)
  }, [])

  return (
    <CommandDialog
      open={open}
      onOpenChange={setOpen}
    >
      <CommandInput placeholder="Type a command or search..." />
      <CommandList>
        <CommandEmpty>No results found.</CommandEmpty>
        <CommandGroup heading="Suggestions">
          <CommandItem>Settings</CommandItem>
          <CommandItem>Messages</CommandItem>
          <CommandItem>Search</CommandItem>
        </CommandGroup>
      </CommandList>
    </CommandDialog>
  )
}
```
Certainly! Here’s a detailed breakdown of the `Command` component and its related types in a table format:

### Command Component Types

#### CommandDialogProps

The `CommandDialogProps` interface extends `DialogProps` from `@radix-ui/react-dialog`.

| Parameter   | Type                       | Description                                                 |
| ----------- | -------------------------- | ----------------------------------------------------------- |
| `...props`  | `DialogProps`              | All properties from `DialogProps` are supported.           |

#### CommandInput Props

The `CommandInput` component uses `CommandPrimitive.Input`.

| Parameter   | Type                                             | Description                                                   |
| ----------- | ------------------------------------------------ | ------------------------------------------------------------- |
| `className` | `string` (optional)                            | Additional classes to apply to the input element.            |
| `...props`  | `React.ComponentPropsWithoutRef<typeof CommandPrimitive.Input>` | All properties from `CommandPrimitive.Input` are supported.  |

#### CommandList Props

The `CommandList` component uses `CommandPrimitive.List`.

| Parameter   | Type                                             | Description                                                   |
| ----------- | ------------------------------------------------ | ------------------------------------------------------------- |
| `className` | `string` (optional)                            | Additional classes to apply to the list element.             |
| `...props`  | `React.ComponentPropsWithoutRef<typeof CommandPrimitive.List>` | All properties from `CommandPrimitive.List` are supported.   |

#### CommandEmpty Props

The `CommandEmpty` component uses `CommandPrimitive.Empty`.

| Parameter   | Type                                             | Description                                                   |
| ----------- | ------------------------------------------------ | ------------------------------------------------------------- |
| `className` | `string` (optional)                            | Additional classes to apply to the empty state element.      |
| `...props`  | `React.ComponentPropsWithoutRef<typeof CommandPrimitive.Empty>` | All properties from `CommandPrimitive.Empty` are supported.  |

#### CommandGroup Props

The `CommandGroup` component uses `CommandPrimitive.Group`.

| Parameter   | Type                                             | Description                                                   |
| ----------- | ------------------------------------------------ | ------------------------------------------------------------- |
| `className` | `string` (optional)                            | Additional classes to apply to the group element.            |
| `...props`  | `React.ComponentPropsWithoutRef<typeof CommandPrimitive.Group>` | All properties from `CommandPrimitive.Group` are supported.  |

#### CommandSeparator Props

The `CommandSeparator` component uses `CommandPrimitive.Separator`.

| Parameter   | Type                                             | Description                                                   |
| ----------- | ------------------------------------------------ | ------------------------------------------------------------- |
| `className` | `string` (optional)                            | Additional classes to apply to the separator element.        |
| `...props`  | `React.ComponentPropsWithoutRef<typeof CommandPrimitive.Separator>` | All properties from `CommandPrimitive.Separator` are supported. |

#### CommandItem Props

The `CommandItem` component uses `CommandPrimitive.Item`.

| Parameter   | Type                                             | Description                                                   |
| ----------- | ------------------------------------------------ | ------------------------------------------------------------- |
| `className` | `string` (optional)                            | Additional classes to apply to the item element.             |
| `...props`  | `React.ComponentPropsWithoutRef<typeof CommandPrimitive.Item>` | All properties from `CommandPrimitive.Item` are supported.  |

#### CommandShortcut Props

The `CommandShortcut` component is a simple wrapper.

| Parameter   | Type                         | Description                                             |
| ----------- | ---------------------------- | ------------------------------------------------------- |
| `className` | `string` (optional)          | Additional classes to apply to the shortcut element.  |
| `...props`  | `React.HTMLAttributes<HTMLSpanElement>` | Additional HTML attributes to apply to the span element. |

#### CommandListGroupDataType

Defines the data structure for items in the command list.

| Property     | Type                                               | Description                                                |
| ------------ | -------------------------------------------------- | ---------------------------------------------------------- |
| `label`      | `string`                                           | The label for the item.                                   |
| `element`    | `React.ReactElement | string`                        | The content to display in the item.                       |
| `onSelect`   | `<T>(arg?: T) => void` (optional)                  | Callback function to handle item selection.               |

#### CommandListGroupType

Defines the properties for the `CommandListGroup` component.

| Property        | Type                                                  | Description                                                                                         |
| --------------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `data`          | `CommandListGroupDataType[]`                         | Array of data items to display in the command list.                                                |
| `group`         | `number[]` (optional)                                | Array of group indices for grouping the data items.                                                 |
| `groupheading`  | `string[]` (optional)                                | Array of headings for each group.                                                                    |
| `onSelect`      | `<T extends string>(arg?: T) => void` (optional)     | Callback function to handle item selection.                                                         |
| `selected`      | `string[]`                                           | Array of selected item labels.                                                                      |
| `className`     | `string` (optional)                                 | Additional classes to apply to the command list group element.                                      |
| `checkabble`    | `boolean` (optional)                                | Whether the items in the list should have checkboxes.                                               |

This breakdown provides a clear view of each component’s properties and types. If you have more specific questions or need additional details, feel free to ask!                 |

## Combobox

You can use the `<Command />` component as a combobox. See the [Combobox](/docs/components/combobox) page for more information.
